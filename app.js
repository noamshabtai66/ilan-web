const blessings = [
  {
    title: "לאילן",
    from: "אורה אשתך",
    photo: "assets/people/ora.jpg",
    text: `איך שעוברות השנים\nואתה בן 54 ונראה כמו בן 30\nצעיר ברוחך קל רגלים\nחתיך איטלקי שקוצר מחמאות בכל מקום\nומבט טוב בעניים\nאבא נפלא\nגבר מתחשב\nבשלן נהדר\nוהכי חשוב טוב לב ❤️\nזכיתי בבעל מדהים\nשרק הולך ומשתבח עם השנים\nמוכיח לכולם כמה אתה עם רצון עז\nאפילו את הסיגריות הנחת בצד\nאז לך אאחל שנים ארוכות ויפות\nשמחות ובריאות\nפרנסה לרוב\nנכדים נכדות ושערות לבנות משמחה\nוהרבה הרבה ברכה כמובן יחד איתי\nאוהבת\nאורה אישתך❤️`
  },
  {
    title: "לאבוש",
    from: "הדרי",
    photo: "assets/people/hadar.jpg",
    text: `לאבוש הכי טוב בעולם\nשתמיד פה בשבילי\nשלוקח אותי שמחזיר\nשיש לו תאוכל הכי הכי טעים\nשתמיד דואג לי ומפנק אותי\nהינה יש לך יום הולדת 54\nלמרות שהייתי נותנת לך גג 40\nזו היזדמנות טובה להדות על הכול\nולהגיד שאני מאוד מאוד מעריכה\nכול מה שאת עושה בישבלי\nואתה עושה המון המון\nאני מאחלת לך אושר בריאות והצלחה\nשתמשיך ליקרוע את כולם בטניס\nבקיצור אני אוהבת אותך המון המון\nמאחלת לך את כול הטוב שבעולם\nמהדרי🩷🩷`
  },
  {
    title: "לאבא",
    from: "אביב",
    photo: "assets/people/aviv.jpg",
    text: `לאבא, המון מזל טוב לרגל יום הולדתך ה-54!\n\nכמו בכל שנה, אני מנצל את ההזדמנות הזו כדי לומר לך כמה אני מעריך אותך. אני רואה את כל ההשקעה והדאגה שלך לכך שלא יחסר לנו דבר, ושתמיד יהיה לנו הכי נוח שאפשר.\n\nאני רואה ומרגיש כמה אתה עוזר ודואג לי ולעתיד שלי; ולמרות שזה יכול לפעמים להיות קצת לוחץ, אני יודע שזה מגיע מהמקום הכי אכפתי שיש, וזה מוכיח איזה אבא טוב אתה וכמה אתה רוצה שאני אצליח ושיהיה לי טוב.\n\nאני מאחל לך המון אושר, כושר ובריאות, ועוד הרבה שנים טובות עם אותה רוח צעירה. שתזכה להרבה הצלחות ולרוות נחת מכל המשפחה, והכי חשוב שתזכה לחיות ברוגע ובכיף, כי באמת כבר הגיע הגיל להתחיל להנות מכל מה שבנית.\n\nבאהבה גדולה, אביב`
  },
  {
    title: "לאילן היקר",
    from: "נועם",
    photo: "assets/people/noam.jpg",
    text: `חג האילנות הגיע ולנו הוא רק מסמל דבר אחד, שיום הולדתך כאן.\nעדיין אותי אילן צעיר שלא קשה לו שום דבר.\nמוקדם בבוקר הוא כבר קם לבשל ולתקתק ואחרי זה ישר לגלריה בצעדי ענק.\nהכל עושה מהלב והנשמה ותמיד כאן לתת עזרה.\nמאחל לך שנה של בריאות, אושר, צחוק, אהבה, עושר, ברכה וסיפוק.\nאמן והחלומות שלך יתגשמו במלואם.\nאני מאחל לך שתמשיך להנות ולטייל בעולם, להיות בעל נאמן ואבא מושלם, איש עסקים לא קטן.\nאני כאן בשבילך לכל בעיה טכנולוגית כמובן,\nולא שכחתי את הטניס בשבתות בבוקר, אבל בוא נודה, אולי יום אחד עוד תעקוף אותי.. או לפחות תנסה.\nמזל טוב ענק,\nנועם.`
  },
  {
    title: "לאילן היקר",
    from: "דולב",
    photo: "assets/people/dolev.jpg",
    text: `יש אנשים שהגיל שלהם הוא רק מספר ואתה ההוכחה הכי טובה לזה.\nבן 54 עם אנרגיות חיוביות ורוח צעירה.\nמאחלת לך להמשיך להנות מהחיים, להגשים מטרות, לצבור חוויות ולהתמלא בסיפוק ושמחה.\nשתמשיך לצעוד לצידה של אורה, שותפתך לחיים ולדרך, בשותפות אמיתית, בעשייה משותפת, בתמיכה הדדית ובהרבה רגעים של אושר יחד.\nתודה על הנתינה האינסופית, על החריצות והמסירות, על כך שאתה תמיד עושה הכל באהבה ובחיוך ואף פעם לא נותן תחושה שקשה או שאין כח.\nוכמובן, אי אפשר שלא להזכיר את האוכל הנהדר והקציצות דגים הטעימות שהפכו לדיבור החם בשולחן.\nמאחלת לך בריאות איתנה, הצלחה בכל תחום, שלווה, שמחה והמון שנים טובות ומבורכות.\nמזל טוב והרבה אושר,\nדולב.`
  },
  {
    title: "לאילן היקר",
    from: "מעיין",
    photo: "assets/people/mayaan.jpg",
    text: `הנה הגיעה עוד שנה\nשבה אנחנו חוגגים לך\nוזה הזמן להודות ולהגיד תודה,\nעל הדגים ועל הקציצות ברוטב לימון,\nעל זה שאתה חושב שאתה מלך הטניס,\nעל הפינוקים לשבת שאתה ואמא מכינים לנו.\nאני מאחלת לך להיות בריא בלי צוואר תפוס,\nמלא ניצחונות בטניס,\nלהמשיך לטייל בעולם ולהגיד שאתה איטלקי,\nלהנות מהילדים,\nלהנות מהקניות בשוק עם הקורקינט החדש שלך.\nוהכי חשוב – תמשיך להיות בדיוק ככה, צעיר בנפש ועם האנרגיות.\nמאחלת לך שנה מלאה בטיולים, וניצחונות (בטניס ).`
  }
];

let currentIndex = 0;
let isTransitioning = false;

// DOM Elements
const heartOverlay = document.getElementById("heart-overlay");
const cursor = document.getElementById("custom-cursor");
const card = document.querySelector(".card");
const titleEl = document.querySelector("[data-title]");
const signatureNameEl = document.querySelector("[data-signature-name]");
const signaturePhotoEl = document.querySelector("[data-signature-photo]");
const progressEl = document.getElementById("blessing-progress");
const linesEl = document.getElementById("blessing-lines");
const nextBtn = document.getElementById("next");
const prevBtn = document.getElementById("prev");
const celebrateBtn = document.getElementById("celebrate");
const musicToggle = document.getElementById("music-toggle");
const musicNext = document.getElementById("music-next");
const bgMusic = document.getElementById("bg-music");
const musicLabel = document.getElementById("music-label");
const dotsEl = document.getElementById("blessing-dots");
const canvas = document.getElementById("confetti-canvas");
const ctx = canvas.getContext("2d");

// Cursor tracking
document.addEventListener("mousemove", (e) => {
  cursor.style.left = e.clientX + "px";
  cursor.style.top = e.clientY + "px";
});

// Double click to like
card.addEventListener("dblclick", () => {
  heartOverlay.classList.remove("animate");
  void heartOverlay.offsetWidth; // trigger reflow
  heartOverlay.classList.add("animate");
  setTimeout(() => heartOverlay.classList.remove("animate"), 1000);
  burst(); // Extra sparkle on like
});

// Bubble Confetti (IG Pop)
let particles = [];
function resizeCanvas() {
  canvas.width = window.innerWidth;
  canvas.height = window.innerHeight;
}
window.addEventListener("resize", resizeCanvas);
resizeCanvas();

class Particle {
  constructor() {
    this.x = Math.random() * canvas.width;
    this.y = canvas.height + 10;
    this.size = Math.random() * 12 + 4;
    this.speedY = Math.random() * -20 - 10;
    this.speedX = Math.random() * 10 - 5;
    this.gravity = 0.5;
    this.colors = ["#f09433", "#e6683c", "#dc2743", "#cc2366", "#bc1888"];
    this.color = this.colors[Math.floor(Math.random() * this.colors.length)];
    this.opacity = 1;
  }
  update() {
    this.speedY += this.gravity;
    this.y += this.speedY;
    this.x += this.speedX;
    if (this.speedY > 0) this.opacity -= 0.02;
  }
  draw() {
    ctx.save();
    ctx.globalAlpha = this.opacity;
    ctx.fillStyle = this.color;
    ctx.beginPath();
    ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
  }
}

function animateParticles() {
  ctx.clearRect(0, 0, canvas.width, canvas.height);
  particles = particles.filter((p) => p.opacity > 0);
  particles.forEach((p) => {
    p.update();
    p.draw();
  });
  if (particles.length > 0) requestAnimationFrame(animateParticles);
}

function burst() {
  for (let i = 0; i < 60; i++) {
    particles.push(new Particle());
  }
  animateParticles();
}

// Bouncy Content Rendering
function renderBlessing(index, direction = "next") {
  if (isTransitioning) return;
  isTransitioning = true;

  const blessing = blessings[index];

  // Fade Out
  linesEl.style.transition = "opacity 0.3s ease, transform 0.3s ease";
  linesEl.style.opacity = "0";
  linesEl.style.transform = "scale(0.95)";

  setTimeout(() => {
    titleEl.textContent = blessing.title;
    signatureNameEl.textContent = blessing.from;
    signaturePhotoEl.src = blessing.photo;
    progressEl.textContent = `Post ${index + 1} of ${blessings.length}`;

    linesEl.innerHTML = blessing.text
      .split("\n")
      .map((line, i) => {
        const trimmed = line.trim();
        return `<span class="line ${trimmed === "" ? "spacer" : ""}" style="--i: ${i}">${trimmed || "&nbsp;"}</span>`;
      })
      .join("");

    updateDots(index);
    updateButtons(index);

    // Fade In (Bouncy Pop)
    linesEl.style.transform = "scale(1.1)";
    requestAnimationFrame(() => {
      linesEl.style.opacity = "1";
      linesEl.style.transform = "scale(1)";
      setTimeout(() => {
        isTransitioning = false;
      }, 300);
    });
  }, 300);
}

function updateDots(index) {
  dotsEl.innerHTML = blessings
    .map((_, i) => `<button class="dot ${i === index ? "is-active" : ""}" aria-label="Story ${i + 1}"></button>`)
    .join("");
  dotsEl.querySelectorAll(".dot").forEach((dot, i) => {
    dot.onclick = () => {
      if (i !== currentIndex) {
        currentIndex = i;
        renderBlessing(currentIndex);
      }
    };
  });
}

function updateButtons(index) {
  prevBtn.disabled = index === 0;
  nextBtn.disabled = index === blessings.length - 1;
}

// Navigation
nextBtn.onclick = () => {
  if (currentIndex < blessings.length - 1) {
    currentIndex++;
    renderBlessing(currentIndex);
  }
};

prevBtn.onclick = () => {
  if (currentIndex > 0) {
    currentIndex--;
    renderBlessing(currentIndex);
  }
};

celebrateBtn.onclick = burst;

// Music Manager (Lo-fi/IG vibes)
const tracks = [
  { name: "CHILL SOCIAL", url: "https://cdn.pixabay.com/download/audio/2021/11/25/audio_b2f9012f2c.mp3?filename=lofi-study-112191.mp3" },
  { name: "CITY VIBES", url: "https://cdn.pixabay.com/download/audio/2022/01/18/audio_d0a13f694a.mp3?filename=lofi-chill-113917.mp3" }
];
let currentTrack = 0;

function updateMusicUI() {
  musicLabel.textContent = tracks[currentTrack].name;
}

musicToggle.onclick = () => {
  if (bgMusic.paused) {
    if (!bgMusic.src) bgMusic.src = tracks[currentTrack].url;
    bgMusic.play().catch(() => { });
  } else {
    bgMusic.pause();
  }
};

musicNext.onclick = () => {
  currentTrack = (currentTrack + 1) % tracks.length;
  bgMusic.src = tracks[currentTrack].url;
  bgMusic.play();
  updateMusicUI();
};

// Initial State
renderBlessing(0);
updateMusicUI();
